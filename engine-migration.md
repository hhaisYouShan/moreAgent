# BossResume compatibility engine

`scripts/agent-loop/**` and `agent-loop-docs/**` were moved from BossResume at
`d400f508ecaeb0cf20b8c6dae7b182af0111ffd2` on 2026-07-13.

They are the active compatibility engine while the portable `src/**` modules
are completed. BossResume's pre-extraction source is protected by tag
`bossresume-before-moreagent-migration-20260713`.

The compatibility engine remains Single-mode by default. M0 is not approved
and Auto remains off.
