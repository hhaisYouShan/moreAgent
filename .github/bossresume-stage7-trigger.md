# BossResume Stage 7 validation V2 trigger

Validation-only branch created from the latest `main`. It runs real BossResume baseline, build/test, policy, Project Map and ephemeral Git Integration validation. It must not be merged.
