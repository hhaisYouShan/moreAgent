{
  "name": "moreagent",
  "version": "0.2.0",
  "private": true,
  "description": "Project-agnostic multi-agent delivery control plane",
  "type": "module",
  "bin": {
    "moreagent": "./src/cli.mjs"
  },
  "scripts": {
    "check": "node --check src/cli.mjs && node --check src/core/state-sources.mjs",
    "test": "node --test test/*.test.mjs",
    "verify": "npm run check && npm run test"
  },
  "engines": {
    "node": ">=20"
  }
}
